# Marginalia · 展示影片 Storyboard + 實作 spec

> 目的:仿 Dribbble 產品展示影片的結構(手機殼 + 螢幕內容 + 標題卡 + 轉場),
> 但不使用 After Effects——改用「透明手機外框 PNG + ffmpeg 合成」。
> 用途:portfolio case study 附件 / 求職接案展示。

---

## 0. 素材準備(你先做,僅這一項需要人工)

- 下載一張**透明背景 iPhone 外框 PNG**(建議來源二選一):
  - Figma Community「Apple Devices」(Andre Soares)— 免費帳號匯出 PNG,款式多
  - Mediamodifier iPhone Mockup Generator — 線上直接生成透明 PNG
- 挑深色系機殼(比較襯米色底 + 墨綠品牌色)
- 把外框 PNG 放進專案(如 `assets/video/iphone-frame.png`),交給 CC
- **操作畫面片段不用你錄** — 見第 1.5 節,CC 用 Playwright 自動錄製

---

## 1. 全域規格(所有標題卡 / 文字統一套用)

| 項目 | 值 |
|---|---|
| 背景色(標題卡) | 米色 `#FDFCFA` |
| 主文字色 | 品牌綠 `#3E4A3D` |
| 手機殼外背景 | 米色 `#FDFCFA`(與 app 背景一致,避免違和) |
| 強調色(僅限展示閱讀痕跡橋段) | 螢光黃 `#F2FF00` / `#FAFF00` |
| 顯示字體(標題,英文) | Cormorant,600 |
| 顯示字體(標題,中文) | Noto Serif TC,600 |
| 說明文字(caption,中英皆可) | Noto Sans TC / 系統無襯線,400,套用現有 `.meta-text` 樣式 |
| 手機殼圓角 / 陰影 | 依所選 PNG 素材原始比例,不額外加濾鏡扭曲 |
| 輸出比例 | 直式 9:16(手機直向),4K 或 1080p 皆可,最終壓縮給網頁用 |
| 輸出格式 | mp4(H.264)+ 另出一份 webm,供 portfolio 網站 `<video>` 用 |

---

## 1.5 CC 自動錄製操作畫面(Playwright)

- 用 Playwright 開啟瀏覽器,視窗設為手機尺寸(比照 iPhone 邏輯解析度,如 390×844)
- 對正式站 `marginalia-ruddy.vercel.app` 逐段錄製,每段對應第 2 節的操作:
  1. 書櫃首頁(All Books 平鋪)— 開啟頁面,靜置數秒
  2. 新增書籍 — 點 Add Book → 輸入書名 → 等搜尋結果 → 點選帶入
  3. 篩選 — 依序點 Reading / To Read / Finished 膠囊,再示範年/月篩選
  4. 筆記與標注 — 進書籍詳情 → New Note → 加截圖 → 標注
  5.(可選)首次導覽 — 清 `marginalia_onboarded` flag 後重載,錄 coach mark 三步
- 用 Playwright 內建 `recordVideo` 選項,每段各自存一支影片檔,檔名對應段落編號
  (01-home.webm、02-add-book.webm…)
- 錄製時畫面只呈現 app 內容本身(無瀏覽器網址列等外殼 UI),乾淨對齊手機殼挖空區域
- 錄完的多支片段交給下一步(第 3 節合成),CC 全程自動跑完,不需人工操作手機

---

## 2. 使用順序(整支影片的敘事流程)

依你 app 實際操作路徑排列,對應到今天完成的功能,不是隨機剪:

```
0) 品牌開場(2.7s)
   已完成的開場動畫直接沿用(打字機 Marginalia → 手繪畫書 → 眨眼小鳥)
   → 這段本身就是成品,直接錄下來當影片第一段,不必重做

1) 書櫃首頁(All Books 平鋪)
   標題卡:「一眼看見所有正在讀的書」
   畫面:預設 All 選中、一列四本平鋪、無 status 分組標題

2) 新增書籍(Google Books 搜尋)
   標題卡:「打書名,自動帶入封面與作者」
   畫面:輸入書名 → debounce 搜尋結果出現 → 點選帶入 → 存入書櫃

3) 篩選(狀態 + 年/月)
   標題卡:「依狀態、依年月,篩出你要的書」
   畫面:切換 Reading/To Read/Finished 膠囊 → 書單 auto-animate 平滑重排;
         再示範年/月篩選

4) 筆記與標注
   標題卡:「在書頁邊,留下你的字」
   畫面:進書籍詳情 → New Note → 加截圖 → 手寫螢光筆標注(此處可用黃色)

5) 導覽 / 首次體驗(可選,若想強調 onboarding 設計)
   標題卡:「第一次打開,也知道從哪裡開始」
   畫面:coach mark 三步導覽快速帶過

6) 收尾
   標題卡:「Marginalia · 為愛寫字的讀者做的閱讀筆記」
   畫面:app icon / lockup 淡出,附上 demo 網址文字卡:
         marginalia-ruddy.vercel.app
```

- 每段畫面時長:3–5 秒(依內容密度微調,篩選/新增書可以到 5–6 秒讓人看清楚)
- 標題卡出現在該段畫面**之前**,獨立 1–1.5 秒黑場後淡入(米色底+品牌綠字),而非疊在手機殼上方
- 段落之間轉場:交叉淡入淡出(crossfade)0.4s,不用花俏轉場

---

## 3. 合成規則(手機殼 + 內容)

- 手機殼 PNG 固定置中,螢幕內容(你的錄影片段)精準對齊挖空區域座標
- 手機殼背後為米色背景(非參考影片的機場散景),維持品牌一致性,不抄襲原素材的視覺主題
- 手機殼下方可放極淡陰影(如果 PNG 本身沒有內建陰影),避免死貼在背景上
- 每段螢幕內容如果原始畫面比參考片段長,ffmpeg 用 `trim` 抓需要的區間,不要整段塞入

---

## 4. 標題卡文字內容(逐字稿,繁中為主)

1. 一眼看見所有正在讀的書
2. 打書名,自動帶入封面與作者
3. 依狀態、依年月,篩出你要的書
4. 在書頁邊,留下你的字
5. 第一次打開,也知道從哪裡開始(可選)
6. Marginalia · 為愛寫字的讀者做的閱讀筆記
   marginalia-ruddy.vercel.app

---

## 5. 交給 Claude Code 的技術任務

- 用 ffmpeg 完成:
  1. 讀入手機殼 PNG(含 alpha 透明通道)
  2. 依序讀入各段螢幕錄影片段,`scale`/`crop` 對齊殼的挖空區域座標
  3. 用 `overlay` filter 合成螢幕內容置於殼下層(殼在上層,因為殼本身含透明挖空)
  4. 每段前插入標題卡(可用 ffmpeg `drawtext` 或先出圖再接影格,若字體渲染效果不佳,
     改用「先產生標題卡靜態圖(html+css 轉圖或直接美術出圖),再用 ffmpeg 接入」)
  5. 段落間 `xfade` 交叉淡出 0.4s
  6. 輸出 mp4(H.264, yuv420p,適合網頁播放)+ webm 各一份
  7. 壓縮至適合網頁載入的檔案大小(建議 <15MB,必要時降解析度或 bitrate)

- 若字體(Cormorant / Noto Serif TC)在 ffmpeg 環境沒有安裝,標題卡改用「HTML+CSS 先產生靜態圖或短片段」的方式製作(可用無頭瀏覽器截圖或現成截圖),避免 ffmpeg drawtext 找不到字型而跑版

---

## 限制

- 不引入品牌色以外的顏色(手機殼底色、標題卡背景、文字色都限定於第 1 節表格)
- 不使用參考 Dribbble 影片的機場背景視覺主題,僅借用「手機殼+標題卡+轉場」的結構
- 黃色僅可用於「筆記標注」那一段畫面本身內容(app 原生行為),不用於任何標題卡或裝飾

---

## 驗收

1) 開頭品牌動畫與最終產品畫面一致(沿用現有 splash,非重做)
2) 手機殼內畫面清晰、無對位偏移
3) 標題卡文案、字體、配色與規格表一致
4) 段落順序符合第 2 節敘事流程,轉場平滑
5) 輸出檔案在手機/桌面瀏覽器可正常播放,檔案大小合理
