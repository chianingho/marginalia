# Case Study 素材:Marginalia · Google Books 串接

> 撰寫日期:2026-07-08
> 用途:8 月正式 case study 的原始素材,狀態為「素材傾倒」

---

**專案名稱:Marginalia · 閱讀筆記**

- 專案類型:個人產品(mobile-first PWA)/ portfolio 主力 demo
- Demo 連結:https://marginalia-ruddy.vercel.app/
- 撰寫狀態:**素材傾倒** / 初稿 / 配圖 / 英文版 / 已發布

## 1. 背景與問題

為自己(也為愛在書頁邊寫字的讀者)做的閱讀筆記 app。原本卡在:新增書籍要手動輸入書名、作者、封面,摩擦大到不會想用。今天解掉的就是這一段——輸入書名就能搜到書、帶入完整資料。

## 2. 我的角色

獨立完成整條鏈路:Google Cloud 專案建立與 API 金鑰安全設定 → API 串接規格設計(交給 Claude Code 實作)→ 環境變數雙軌配置(本機 + Vercel)→ 本機與 iPhone 實機驗收 → 部署上線。全程不寫一行手工 code,但每個技術決策都是我下的。

## 3. 解法與架構

```
使用者輸入書名(debounce 300ms)
  → Google Books API(帶受限 API key)
  → 整理回傳資料(封面 http→https、無封面 fallback、無作者 fallback)
  → 搜尋結果列表 → 點選帶入 → 存入 localStorage(Supabase 接手前的過渡層)
```

白話版:使用者打字停頓 0.3 秒後才發出查詢(debounce = 防止每打一個字就查一次,省流量也省配額);拿到的書籍資料先「清洗」過——封面網址改成加密協定否則 iPhone 會擋、沒封面的書給一塊奶油色底的書名首字方塊——才顯示出來。

## 4. 關鍵決策

- **決策一:申請 API key 而非匿名呼叫。** 原以為搜尋端點免 key,實測發現 Google 已把匿名配額降為 0(`quota_limit_value: "0"`)。與其等政策鬆動,直接申請免費 key 一勞永逸。
- **決策二:key 公開,但用雙重限制讓公開無害。** 前端 app 的 key 必然暴露在瀏覽器,所以不追求藏,而是在 Google Cloud 設網站限制(只有我的網域能呼叫)+ API 限制(只能查書)。驗證方式很有趣:瀏覽器直打 API 網址被擋(referer 為空),反而證明防護生效。
- **決策三:拿掉 `langRestrict=zh-TW` 語言過濾。** 該參數對中文書過濾不穩定,常把存在的書濾成 0 筆;改為靠使用者輸入的中文關鍵字自然帶出中文結果,搜尋成功率明顯較好。
- **(備用)決策四:環境變數雙軌供應。** 本機讀 `.env`(gitignore 保護)、線上讀 Vercel Environment Variables,金鑰不進版控;並注意 Vite 變數是 build 時打包,須在部署前設定完成。

## 5. 成果

- 新增書籍流程從「手動填 4 個欄位」變成「打書名 → 點一下」
- 當日完成:API 申請與安全設定 → 串接 → 部署 → iPhone 實機驗收全鏈路
- Demo:https://marginalia-ruddy.vercel.app/(手機開啟體驗最佳)

## 6. 學到什麼 / 下一步

**學到:**

- 「文件說免 key」不等於「現在免 key」——API 政策會變,實測為準
- 錯誤訊息是地圖不是路障:429 的 `quota_limit_value: "0"` 和 403 的 `referer <empty>` 各自精準指出下一步
- 前端金鑰的安全模型:不是藏起來,是讓拿到也沒用

**下一步:**

- 7 月:mobile UI 改版 → PWA 設定
- 8 月:Supabase Auth + 資料上雲;新功能欄位設計併入 schema,一次到位
  - 筆記獨立頁(時間流筆記牆,與書籍頁互通)
  - 閱讀狀態(想讀/在讀/讀完)+ 自動記錄開始/完成日 → 時間區間
  - 排序切換(最近新增/最近有筆記)+ 狀態篩選;日期篩選待資料量大再做

---

## 附註:接案版開場素材

客戶問題:資料輸入摩擦大,使用者不願手填 → 解法:第三方 API 串接 + 金鑰安全設定 → 成果:一鍵帶入,流程從 4 個欄位變成 1 次點擊。此結構可直接對應 case study 骨架(客戶的問題 → 架構決策 → 白話架構說明 → 結果)。
